// Theo (Tech-Support chat) module entry. Wraps the module's TechSupportApp.

import 'package:flutter/material.dart';
import 'main.dart' show TechSupportApp;

class TheoEntry extends StatelessWidget {
  const TheoEntry({super.key});
  @override
  Widget build(BuildContext context) => const TechSupportApp();
}

import 'package:flutter/material.dart';
import 'main.dart' show HestiaApp;

class HestiaEntry extends StatelessWidget {
  const HestiaEntry({super.key});
  @override
  Widget build(BuildContext context) => const HestiaApp();
}
